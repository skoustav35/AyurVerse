# build artefacts
node_modules/
dist/
.vite/
*.tsbuildinfo

# logs / editor
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.idea/
.vscode/

# secret sandboxing (NEVER commit keys)
.env
.env.*.local

# arena wiring (managed by the platform, would just confuse git)
.vercel/
.agon-env/

# debug / harness artefacts (not source)
*.png
core.*.dump
core.[0-9]*
hprof.txt

# arena-generated wiring files (auto-managed, contain secrets)
vercel.json
