# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# dependencies (the only thing excluded — reinstall with `npm install`)
node_modules

# build output
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NOTE: this is a PRIVATE repo — .env and vercel.json ARE committed on purpose
# so it can be hosted on Vercel directly. Keep the repo private.
