AyurVerse super-pass: resilience, AI society harness, security arc, scale pass, Threads + Vaidya polish

- db resilience: survive rotated/paused keys; aux media storage lane; society observatory
- 500-persona AI society (bots/ + one-file ayurverse_society.py: forge, comments,
  likes, saves, follows, circles incl. writing in them, golden threads/replies)
- security: identity-forwarding API (ALS scope), RLS suite v2, strict headers/CSP,
  PKCE, secrets out of source
- scale: paginated threads + load-older, edge caching for anon reads, realtime
  micro-batching, code-split heavy views
- threads: optimistic sends + retry, per-thread drafts, inbox search, unread
  divider, lightbox, voice waveforms, per-bubble timestamps
- Vaidya: AVS gateway; fixed house mind branded Sarvam 105B M (deepseek-flash,
  reasoning high); summonable on any tab; animated streaming answers
- avatars: CSP allows googleusercontent; profile row is the truth source;
  letter-mark fallback never shows broken glyphs
- vercel.json + .env INCLUDED; service keys are placeholders by GitHub policy —
  set them via the platform Secrets tab
